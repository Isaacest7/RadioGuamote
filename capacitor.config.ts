import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.puce.radiolavozdeguamote',
  appName: 'Radio1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
